# Claude 2026-09-21: retired — DB resume mirror dropped (sidecar-only).
# Reason: usenet_resume_state is DROP'd by migration 0028; ExecStart would VACUUM
#   a missing relation. Sidecar .sakms-resume.json is SoT.
# Troubleshooting: unit kept for history; do not enable on the host.
# Review if: file can be deleted after operators have disabled the live timer.
# Related: /usr/local/bin/sakms-resume-vacuum.py; sakms-resume-vacuum.timer
#   internal/db/migrations/0028_drop_usenet_resume_state.sql

[Unit]
Description=sakms resume-state stale cleanup (RETIRED — usenet_resume_state dropped)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
# Retired 2026-09-21: DB resume mirror gone; do not VACUUM usenet_resume_state.
# ExecStart=/usr/local/bin/sakms-resume-vacuum.py
